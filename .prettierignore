# Framework-owned session-protocol files, synced verbatim from ~/code/ai by its
# INSTALL.md; formatting them would diverge from the source of truth.
.session.md
AGENTS.md
CLAUDE.md

.DS_Store
.moon/
CHANGELOG.md
coverage/
dist/
mise.lock
node_modules/

# Rollup's TypeScript config plugin creates this transient file while packaging.
# Moon CI runs packaging and formatting concurrently, so Prettier must not race
# the file's creation and removal.
rollup.config-*.mjs
